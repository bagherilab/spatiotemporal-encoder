[tox]
isolated_build = True
envlist = py{311}, linter, mypy
skipsdist=True

[testenv]
deps =
    pytest
    pytest-cov
commands =
    python -m pytest --cov-report html --cov=src/ tests/

[testenv:linter]
deps =
    black
    pylint
commands =
    black -l 100 src/ tests/
    pylint --ignore-patterns=test.*?py --fail-under=7.0 src/ tests/

[testenv:mypy]
deps =
    mypy
commands =
    mypy --config-file mypy.ini --disallow-untyped-calls --disallow-untyped-defs --disallow-incomplete-defs src